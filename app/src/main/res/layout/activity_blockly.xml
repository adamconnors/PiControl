<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.cromulator.picontrol.BlocklyActivity">

    <include layout="@layout/blockly_unified_workspace" />

    <!-- Web view for camera stream -->
    <WebView android:id="@+id/webview"
        android:layout_height="@dimen/webview_width"
        android:layout_width="@dimen/webview_height"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_margin="6dp"/>


</RelativeLayout>
